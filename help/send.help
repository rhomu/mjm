Send job using screen. Usage:

   mjm send command

or

   mjm send -n name -p priority command

where priority can be very-high, high, normal, low, very-low. The job is sent to
a newly created session which can be reattached with the attach command.
